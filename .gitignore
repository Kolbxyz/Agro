/target
# The deploy build's target dir, written by the Debian container. Build output, not source.
/target-deb

# The live database. Holds account passphrases and API keys — never commit it.
agro_data.db
*.db-wal
*.db-shm

# Built into the binary by rust-embed at compile time, not source.
dashboard/dist/
dashboard/node_modules/
